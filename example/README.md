# example

React Native 0.87 演示工程，依赖通过 `file:..` 指向本仓库。库只有 Android 实现，请用 Android 运行。

```bash
cd example
npm install
npm run android
```

改完库的 TypeScript 后，先在仓库根目录执行 `npm run build`，示例里的 `lib/` 才会更新。
